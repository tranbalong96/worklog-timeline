# Phase 5 Prompt - Settings

Read CLAUDE.md and .claude/*.

Implement Phase 5 from .claude/phase-plan.md.

Scope:
- Implement Settings page.
- General settings:
  - default work hours per day
  - week start
- AI settings:
  - enabled
  - provider
  - API key
  - base URL
  - model
  - temperature
- Apply provider defaults when provider changes.
- Save settings to AppData and localStorage.
- Show security note that API keys are stored locally in the browser.

Do not implement actual AI generation yet.

After implementation:
- Run npm run build.
- Fix errors.
- Summarize changed files.
